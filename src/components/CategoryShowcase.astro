---
import { showcaseCategories } from '../data/showcase-categories';
---

<section class="py-16 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-12">
      <h2 class="text-4xl font-bold text-[#412A1D] mb-4">
        Explore Our Collections
      </h2>
      <p class="text-xl text-[#412A1D] max-w-3xl mx-auto">
        Discover our carefully curated furniture collections designed for every room and style preference in your home
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {showcaseCategories.map((category) => (
        <div class="group cursor-pointer">
          <div class="space-y-4">
            <a 
              href={category.href}
              class="block relative aspect-[4/3] rounded-[2rem] overflow-hidden bg-gray-100"
              aria-label={`View ${category.title} collection`}
            >
              <img
                src={category.image}
                alt={category.title}
                class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
              />
            </a>
            <div class="text-center px-4">
              <a 
                href={category.href} 
                class="inline-block text-gray-600 text-xl font-bold group-hover:text-gray-900 border-b-2 border-transparent group-hover:border-gray-900 transition-all duration-300"
                aria-label={`View ${category.title} collection`}
              >
                {category.title}
              </a>
            </div>
          </div>
        </div>
      ))}
    </div>

    <div class="mt-12 text-center max-w-2xl mx-auto">
      <p class="text-gray-700 text-lg">
        From children's rooms to luxury suites, our carefully curated collections offer the perfect blend of style, comfort, and functionality. Each piece is thoughtfully designed to enhance your living space while meeting the unique needs of every family member.
      </p>
    </div>
  </div>
</section>