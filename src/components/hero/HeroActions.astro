---
import QuizCard from '../quiz/QuizCard.astro';
import type { Locale } from '../../consts';

interface Props {
  currentLocale: Locale;
  exploreText: string;
}

const { currentLocale, exploreText } = Astro.props;
---

<QuizCard {currentLocale} />